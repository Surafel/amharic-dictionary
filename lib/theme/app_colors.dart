import 'package:flutter/material.dart';

/// Palette sampled from a reference dictionary app screenshot
/// (dark navy header, mid teal accents, charcoal body text).
class AppColors {
  AppColors._();

  static const navyDark = Color(0xFF0F3750);
  static const tealMid = Color(0xFF275668);
  static const tealAccent = Color(0xFF4A7C95);
  static const bodyText = Color(0xFF2F3336);
}
