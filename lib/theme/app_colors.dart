import 'package:flutter/material.dart';

/// Palette sampled from the app icon (leather-bound book, gold trim,
/// parchment page, warm sepia ink).
class AppColors {
  AppColors._();

  static const bookBrown = Color(0xFF5C4630);
  static const goldMid = Color(0xFF8C7657);
  static const goldAccent = Color(0xFFB8925A);
  static const parchment = Color(0xFFFBF6EC);
  static const bodyText = Color(0xFF3A2E22);
}
